{
  "name": "temps-acceptance-tests",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Release E2E acceptance suite. Deterministic steps + Claude-as-judge.",
  "scripts": {
    "run": "bun run runner/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "yaml": "^2.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
